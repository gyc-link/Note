
| 单词                   | 音标  | 注释                           |
| -------------------- | --- | ---------------------------- |
| correspond           |     | 符合；相一致                       |
| comma                |     | 逗号                           |
| numerator            |     | 分子                           |
| denominator          |     | 分母                           |
| cutter               |     | 刀具、切割器具                      |
| distinct             |     | 明显不同的，不同种类的，完全不同于某事物，清晰的，清楚的 |
| no need to mope      |     | 别锤头丧气                        |
| tunnel               |     | 隧道（汽、火车、动物的）挖掘、钻洞            |
| slink away/sneak off |     | 悄悄溜走、偷偷离开                    |
| march off            |     | 大步离开                         |
| whine                |     | 哀诉、嘀咕                        |
|                      |     |                              |

~~小件配料表也需要进行坡口信息反填~~

~~坡口信息也填在配料备注这一列中，那么这一列的位置应该放在小件配料表的哪里？~~
~~主件配料表里因为有配料这列，所以放在了它的旁边~~

~~坡口信息是不是小件本身就有的信息？ 不是~~
~~是画图的人后面额外添加的信息~~
~~因此我选择放在最后面~~

~~问题：~~

~~1、小件配料表 配料备注列所在位置~~

~~2、主小件配料表跟主小件放样表的关系？~~
~~因为刚从分类材料统计表出来的时候，这两者一致的。~~
~~更新配料表后，放样表不更新~~
~~如果只是对照，那更新配料表后多出来的备份又有什么意义？~~

~~3、制作分类统计表，有重复代码，为什么不抽出公共部分代码~~
~~比如 小件配料表跟小件放样表其实是本来就是复制关系，小件配料表中多了一个记录附件循环，其余代码一致。~~
~~为什么这里不使用copysheet功能，将其中一个表复制到另一个表里，而是选择复制代码？~~

~~4、~~
~~制作分类统计表，用的表头~~
~~跟后面更新配料表 用的表头 为什么不一致？    先问~~


~~清单有很多重复的情况，需要进行代码复制，这是没法避免的吗？~~

~~5、启用自动坡切和内缩~~
~~用户应该更看重每个坡口类型，有不同的板厚范围和坡口最大角度限制~~
~~是不是可以把启用自动坡切和内缩这个选项做成控制所有的坡口类型，而不是每个坡口类型都单独配一个这个选项。~~
~~另外，用户是不是需要单独控制某一个坡口类型是否能够自动坡切和内缩？~~


~~应用按钮 将目前的设置保存并应用~~
~~恢复按钮 抛弃当前的设置，恢复到上次保存的全局设置~~

~~板厚范围 8-26这个默认的文字是否需要删除？不删把 ，可以当成例子~~

~~恢复默认选项会生成一个新的option，且这个option是默认的配置，那么初始设定需要考虑下是否合理，因为①坡口只在30-80，80-120 这两个区间内才能开坡口，初始设定可以取30-80~~

~~坡口①的height属性，其中有一个系数是跟板厚有关的，并且必须满足板厚在某几个区间内，才能算出对应的系数，那么如果板厚不在该范围内，系数出不来，这是基础设施不满足，因此出排版图就不成功。~~

~~把四种坡口类型的绑定信息全部做出来~~
~~优点：用户使用友好，直观~~
~~缺点：扩展较难，如果以后有其他坡口类型，可能需要额外做一个窗体~~

~~用一个下拉菜单做绑定信息~~
~~优点：扩展容易，~~
~~缺点：不够直观，需要先选择哪一个坡口信息，才能设置对应的绑定信息。~~

~~新设计一个窗口，由选项-放样图设置中的坡口信息绑定跳转而来，在这个窗口中进行各个坡口类型的信息绑定。~~
~~优点：直观，不拥挤，易于维护~~


怎么通过反射拿到的标签信息
怎么拿到坡口类型对应的标签？
1、直接根据已有的标签-type字典，反查得到标签
2、利用反射，创建每一个与目标相关联的特性的实例，然后拿到这个实例的属性
方案二频繁创建自定义特性实例，会影响性能，好处是比较直观
方案一是直接查表，可能只根据表的注释很难知道这个表里的具体内容是啥

另外，已有的标签-坡口类型字典是通过反射，然后筛选后创建的


1、关于启用自动坡切和内缩
用户是不是希望控制那个坡切类型的能自动坡切，哪个类型不能自动坡切？
比方说，当天机器都用于自动坡切①类型了，其余的类型都需要人工坡切？

2、如果是新增窗体，那么是在图纸编辑窗体的子窗体，还是通过新写一个命令，然后出现一个新的窗体？

如果是新写一个命令，那么命令名，下拉菜单栏，命令的位置怎么定

如果是子窗体，那么当用户设置完成后，怎么退出？需不需要恢复按键？
如果子窗体，有 应用，恢复，退出，那么结束后，主窗体是否还需要按应用？
应该是不管点应用还是点退出，都不影响子窗口。同样，子窗口也不影响主窗口。
按windows的系统，有一些子窗体里面有单独的应用设置，有一些只有确认和取消

子窗口 设置坡口自动开坡的限制参数，点击应用，应该是已经生效，如果不是应用而是确定，就需要主窗口再点击应用

界面从上到下设计，这样用户用起来也不易出错。



关于命名，目前我觉得最好是对应，一眼能看出来联系。


当整批零件均为矩形时，就没有排版零件图，就不会触发更新配料表功能，此时如何更新更新配料表并相应缩减宽度

说明书里
更新配料表
前置条件：读取dwg文件，获取所有的排版图

排版图是为了什么？是为了省料，如果没有排版图，对于一些奇形怪状的主件，人很难充分利用一块料。
用户的问题是，没有排版图，又是矩形，意味着矩形不出排版图，用户觉得没有排版图，更新配料表不会触发。也就说，用户认为，更新配料表必须要有排版图。
目前更新配料表功能，对于没有主件排版图，确实不会更新配料表
也就是说，原来配料表是需要主件排版图这个文件的，没有这个文件，就不执行更新主见配料表。
现在要额外支持：当没有主件dwg文件时，也要更新。
具体到代码
mainDict != null && !UpdateMainNesting(excelPackage, checkPackage, mainDict, checkPrecision))
也就是说，mainDict !=null 这个条件限制的太死了或者说，mainDict可以为null了
我将maindict为null的情况也输入了进去，那么，假设用户以为这批次的零件都是矩形，但是清单里有一个不是矩形，但是又没有图纸作为辅助识别出这个零件（有没有排版图）是不是异形；这个时候，需不需要程序找到这个零件并提醒用户？

使用该功能，必须保证出排版图的是异型。

出排版图，什么形状的都可以出，用户自己决定

我刚刚问的问题：是不是只有异形才出排版图，这个问题其实绕了一下，应该直接问，更新配料表功能中，给零件图设为异形的前置逻辑，是不是默认排版图都不是矩形。
所以刚刚没有为对面的人考虑，他不知道我现在考虑的问题是啥，我应该先把前提说出来。
是更新配料表功能。

对于该功能，将出了排版图的设定为“异形”
这个逻辑是否可靠，正确


4.1 首先矩形尺寸自动缩减功能（宽度）只针对
1、矩形
2、主件
3、腹板
4、备注
因此零件规格如果是拆分成PL的情形，理应完成

矩形尺寸自动缩减功能，在更新配料表时，目前“PL矩形零件”可以自动缩减零件宽度，当零件规格是型钢截面拆分成PL的情形，腹板宽度没有相应缩减

RECT怎么标记的？
1、拿到所有的排版图
2、拿到配件清单的所有零件号
3、找到排版图中零件号在清单中的，记为group
4、那么清单中，出去group的标记为rect。
所以不是，出排版图的标记为异形，其余的就标记为RECT。
而是 清单中存在、未出排版图、标记为Rect

小件排版图如果是空的，那么小件配料表中的数据会被清空。




所以问题4.2是不是用户理解错了？
首先我测试，没有排版图，矩形零件也能自动缩减
然后说明书里说，必须要先读取排版图，是不是意味着，用户并没有图纸？

用户现在是在更新配料表这一步骤时， 矩形主件不出排版图。
有一种情况是整个批次都是矩形的主件
没有排版图——>
1、没有dwg文件吗
2、有dwg文件，但是没有排版图

情况1，确实不更新，情况2，能自动更新配料表，且矩形尺寸自动缩减也能执行。

所以，现在配料表功能需要至少一个dwg文件（主小件坡口图/排版图）和一个excel表
这个至少是什么逻辑？
主见可以没有，或者小件可以没有？
按一般设计，当有一个选项是空的时候，应该不执行。
这个设计我可以理解为整个批次，可能只有主件，或者只有小件
现在有一种情况是，主件没有排版图，

这个批次都是矩形主件
这个批次主件都是矩形，有小件，目前功能可以执行，但是不更新主件配料表
这个批次都是小件
这个批次都是主件（含异形）
这个批次有主件，有小件。

解决一个问题
整个批次都是矩形主件和小件

如果我设置读取主件排版图永远不为null
那么当主见清单真的是空的时候会发生什么
会出现一张空的主件审核表

放宽条件，由原来的至少选择一个dwg文件，到可以都不选择。
加一个提醒，防止用户日常工作中，点错了，只点击了材料表，而没有把两个dwg文件也选进去。






没有小件排版图的结果是，小件配料表会被清空。可能这个批次就没有小件
所谓以防万一，为null时不更新小件配料表。




软件实体（类、模块、函数等）应该对扩展开放，对修改关闭

每次访问属性的getter，都会执行一次get方法，
注意，比如一个判断条件为
value>5&&value<10 

get方法里做了计算，比如乘以2
public double Value
{
    get
    {
        return CalculateValue();
    }
}
那么如果一开始value是4，左边的判断 4* 2满足，然后变成 8* 2 不满足，会引起疑惑，最好缓存，别直接访问getter
value = obj.value

关于引用类型的赋值，如果引用类型的属性都是引用类型，改了一个就相当于改了另外一个
如果有些属性是值类型，那么改了一个，另一个不会受影响的。
因为值类型赋值时，是复制了一份过去的。

类里面有值类型和引用类型

父窗体的恢复会影响子窗体，合理吗
但是子窗体的恢复如果影响父窗体，肯定不合理。

原来代码里为什么要重新赋值一次？因为中间，恢复的功能是通过新建一个实例实现的。




为什么配置项的全局选项做成静态字段而不是属性（单例）？

坡口标注的形式是多重引线，引线的箭头指在轮廓上，说明这个轮廓线需要开坡口
遇到需要断料的，坡口标注指在断料符号上。

点击子窗体的应用，就是理解将当前页面的设置进行最终的选项配置，父窗体点击退出不应该取消这个配置。
点击恢复，只恢复当前页面，不会恢复父窗体，而父窗体的恢复是能恢复子窗体
所以，将子窗体的恢复设置成，恢复当前页面

根据板厚和最大角判断是否能自动开坡口
1、用在轮廓线
2、用在断料处

感觉逻辑有点混乱，前面已经定义了超过板厚和最大角不应该把数据存到extendData中，但是后用到了extendData后还是会去判断是否在板厚范围内

当坡口标注中的角度存在多个时，不做数控切割；

为什么canautobevel函数里，不把开启自动切坡选项也放进去？
1、原来就是分开的
2、这个函数应该更看重这个板子是否能自动开某个坡口类型，而不是人为的设置

struct
如果不需要实现继承、不需要实现接口，可以用struct
不能太大

读表
从哪里开始读，读到什么样的数据，遇到不合规定的数据怎么处理
现在的逻辑时读数据，读到空行或者注释行就结束，遇到不是规定的数据或者填写不完整的数据返回失败
方便调试，如果出了问题知道是什么情况

读表得到的double值是千奇百怪的，因此不要指望double的相等，要用误差

状态机是什么？

结论先行
1、我到底想表达什么
2、我的理由是否有逻辑
3、别人能不能快速理解

反过来是过程式表达，人的大脑更容易接受中心观点，而不是大量信息，最后才知道结论

背景、冲突、问题、答案
相互独立、完全穷尽
归纳、演绎
具体到一般、一般到具体





#csharp-flashcards 

递增运算符和递减运算符，不管是++，--前置还是后置，语句执行之后，操作数的指是相同的，如果把 x++赋值给一个变量y，那么y变量是x之前的旧值，而不是加1后的值，如果是 ++x，赋值给y，那么是加1后的值
x+=1 如果赋值也是赋的新值，并且灵活，不只是递增1。

？？空合并运算符

throw ex 会清空并重置调用栈，把出错点错误地标记在throw ex 这一行

值类型和引用类型
在函数传入，返回这一块儿最能体现他们的区别
值类型不管是作为实参传入函数，还是作为函数的返回值，总是复制一份儿，不管这个值类型在哪里（栈或堆上）
而引用类型，总是把内存地址传入或者返回一个指向引用类型实例的内存地址

如果值类型作为集合的元素，那么它在堆上，是不是很难找到值类型实例在堆上的具体位置？

tryget 有一个默认不抛异常的心理作用。

为什么不能声明为静态结构体
1、设计冗余
2、值类型冲突，值类型可以通过复制传值，而静态类强调全局唯一性，不应该创建实例

selectmany
将嵌套的“集合的集合”扁平化（Flaten）为一个单一的一维集合，并支持在一对多关系中进行投影转换。
比如一个字典的集合，flaten后，就是每个字典的所有pair组成的一维集合

epsilon
一般代表极小的正数、误差或特定参数

数学上 任意小的正数