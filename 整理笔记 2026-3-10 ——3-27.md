
## 一、数据结构相关

### 1. 数组（十字链表）

- 计算机可直接跳到任意索引位置 ::基于内存地址跳转 + 计算 直观感受 带索引的方括号
- 但数组不适合插入 / 删除操作。
- 三元组表：通过`#define MAX 10`定义宏，结合结构体存储稀疏矩阵的行、列、值信息。
	typedef struct 
	{ int i,j,v  }node  
	typedef struct
	{  int m,n,t  
	node data[MAX]} mat
- 十字链表：节点包含行、列、值，以及指向下方和右侧节点的指针（`down`、`right`），适配稀疏矩阵存储。

### 2. 散列表（Hash table）

- ~~核心形式：键 - 值对，别称包括散列 / 映射 / 字典 / 关联数组。~~
- ~~散列函数，将字符串转为数字的过程为散列，用于对照的密码是散列函数。函数就是一个输入对应一个输出，但同一个输出不一定对应一个输入。所以会有冲突。这里的输入和输出就是键-值~~
- ~~核心流程：计算散列值 → 定位对应格子 → 线性查找解决冲突。~~
- ~~关键概念：散列函数，负载因子（数据量 / 格子数），扩展机制（如每增 7 个数据扩 10 个格子），适合检查数据存在性（如计票场景）。~~
- ~~时间复杂度极低，对于读取和插入来说。~~

### 3. 栈和队列、递归、快速排序

- 栈（LIFO）：处理顺序与接收顺序相反，典型场景为括号匹配（左括号压栈、右括号匹配出栈）。
- 队列（FIFO）：处理顺序与接收顺序一致，双向链表作为底层时，头删 / 尾插时间复杂度 O (1)。
- 递归：需定义基准情形，计算机通过栈记录函数调用，无限递归会导致栈溢出；递归不改变算法大 O 复杂度。
- 快速排序：时间复杂度 NLogN，核心是 “分区” 操作。

### 4. 二叉树

- 基础概念：树是基于结点的数据结构，树里面的每个结点，可以含有多个链分别指向其他多个结点。1-N
- 二叉树规则：每个节点子节点数为 0/1/2；若有两个子节点，左子节点值 <父节点，右子节点值> 父节点。
- 性能：平衡二叉树查找复杂度 logN；完全二叉树平均查找==步数==约为树高 - 1。
	1+2*2+3*2^2+4*2^3+..+k*2^(k-1)  
	错位相减求得（h-1）2^h +1  
	则平均步数为上式除以 N =2^h -1 h较大时，近似等于h-1步
- 删除逻辑：1、若被删节点有两个子节点，需找其后继节点（右子树最左下节点）。2、若被删节点只有一个子节点或无子节点，直接将其子节点（或null）替换到删除位置。

## 二、编程语法相关

### 1. C 语言

- `typedef`：为已有类型起别名（如`typedef int myint`），结构体内部可引用自身指针（如链表节点`struct node *next`）。
- 输入输出：`scanf("%d%d", &ms, &ns)`读取两个整数存入对应变量。
- 指针操作：`*p=3`为指针指向的存储单元赋值；`malloc`开辟内存并强制类型转换（如`q=(szjd*)malloc(sizeof(szjd))`）。
- 循环控制：`continue`跳过当前迭代剩余部分；C# 可给外层循环加标签，通过`continue 标签`跳出外层循环。

### 2. C# 语言

#### （1）对象、类型、传递方式

- ~~核心区分：变量是存储位置的==名称==（值类型存数据本身，引用类型存指针）；对象是内存中的实际==数据==（类的实例）。~~
- ~~内存分配：值类型（栈 / 嵌入对象内部）、引用类型（堆）；数组 / 字符串为引用类型。~~
- ~~传递方式：~~
    
    - ~~值传递（默认）：形参是实参副本，修改形参不影响原变量（但可修改引用类型指向对象的内容）。~~
    - ~~引用传递（`ref`）：形参是实参别名，可修改原变量指向的对象。~~
    - ~~输出传递（`out`）：强调输出，调用前无需初始化，方法内必须赋值。~~
- ~~补充：~~
	- ~~通常值类型不用引用传递，原因在于这个流程不一定比值传递快（复制一份数据给形参，即使该数据大，也比解引用要快）~~  
	- ~~引用类型使用值传递，有效率高的好处，因为不需要复制。~~
	- ~~变量的生命周期通常由它的作用域（大括号）决定  （方法执行完毕，局部变量就消失了）。  对象的生命周期由垃圾回收器（GC）管理。即使指向它的变量没了，只要还有别的变量引用它，对象就依然存活。~~

#### ~~（2）类的深入理解~~

- ~~引用类型对象在堆上，变量（引用）在栈上；实例成员每个实例独有，静态成员所有实例共享（无需创建实例即可存在）。~~
- ~~访问修饰符：`private`/`public`/`protected`/`internal`/`protected internal`。~~

|          修饰符           |          权限范围          | 严格程度 |
| :--------------------: | :--------------------: | :--: |
|      **private**       |     只有**当前类自己**能访问     | 最严格  |
|     **protected**      |    当前类 + **子类**能访问     |  严格  |
|      **internal**      | **同一个项目 / 程序集**内所有类能访问 |  中等  |
| **protected internal** |  同项目 **或** 子类（满足其一即可）  |  宽松  |
|       **public**       |      **任何地方**都能访问      | 最宽松  |
- ~~类成员：字段、方法、常量、属性、构造函数、析构函数、索引、事件、运算符。~~
- ~~字段有默认值，引用类型默认值是null~~
- ~~常量：`const`（编译时常量，无存储位置，编译期替换）、`readonly`（运行时常量，可实例 / 静态，引用不可变但内容可变）。~~
- ~~属性：函数成员，`get`/`set`方法；~~
	- ~~只读自动属性编译为`readonly`私有字段 + 只读访问器。~~
		- ~~可以有静态属性~~
	- ~~只读属性：只有get访问器，只限制==属性赋值==，只读属性的返回值如果是集合，则内容==可以==修改~~
- ~~索引器：实例成员，无名称（用`this`），可像数组一样访问类成员。索引器总是实例成员~~
	- ~~支持多参数（如`int this[string subject]`）~~
	- ~~可以重载索引器。~~
	- ~~索引可以是任意类型~~
- ~~构造 / 析构：构造函数无返回值、与类名相同；静态构造函数无参数（无参数，所以只能有一个），用于初始化静态字段；析构函数释放资源。~~
- ~~`partial`分部类 / 方法（Windows Form）；`this`关键字解决字段与形参重名。~~
- ~~问题：静态成员在被使用前就会被初始化（如果有初始化语句），不创建实例，静态成员也存在，即静态成员不需要创建实例。那么静态成员的生命周期？~~

#### ~~（3）元组~~

- ~~轻量数据结构（`system.ValueTuple`），可组合多值为一个对象。~~
- ~~用法：支持命名字段（`var person = (Name: "张三", Age: 30)`）或默认字段（`Item1/Item2`）；可作为方法返回值，支持解构（`var (total, cnt) = Calculate(...)`）。~~

#### ~~（4）结构体（struct）~~

- ~~值类型：变量独立，赋值时复制全部值（区别于类的引用复制）；隐式密封，不能派生。~~

#### ~~（5）方法与类型转换~~

- ~~方法：有返回值的方法所有代码路径需以`return`/`throw`结束；支持命名参数和位置参数。~~
- ~~转换：`as`运算符转换失败返回`null`；~~

#### ~~（6）继承~~

- ~~规则：==类仅一个直接基类，可实现多个接口==。~~
- ~~隐藏基类成员：派生类同名成员可加`new`提示编译器；通过`base`访问被隐藏成员。~~
- ~~虚拟方法：基类`virtual`声明，派生类`override`重写。~~
- ~~扩展方法：为密封类扩展功能，声明在静态类（所有成员都是静态的，也是密封类）中，第一个参数加`this`+ 目标密封类类名，通过密封类实例调用。~~
#### ~~（7）其他~~
- ~~嵌入式语句不能直接是声明 / 标记语句（需加大括号）。~~
- ~~null 是引用类型的“空引用”，表示一个引用变量没有指向任何对象~~
- ~~`new`创建结构体返回实例本身；~~
- ~~静态类是隐式密封的。~~

## 三、算法相关

### 时间复杂度

- 核心问题：数据增长时算法步数的变化趋势，区分常数 / 线性 / 对数 / 二次时间。
- 排序算法：
    
    - 冒泡排序：比较次数`(n-1)+(n-2)+...+1`，最坏情况每次比较都交换。
    - 选择排序：比较次数同冒泡，每轮最多 1 次交换（记录最小值索引）。
    - 插入排序：最坏情况比较次数`1+2+...+n-1`，涉及移除 / 比较 / 平移 / 插入操作。
    
- 优化思路：==如两数组求交集，找到匹配元素后`break`跳过后续比较==。




