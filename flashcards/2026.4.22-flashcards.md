
#cad开发-flashcards 

更多考虑用==1;;向量==去手算新的点的坐标，问题是这个点发生了什么变化才到了新的位置。
例如，延长圆弧的终点和起点，就是沿着圆曲线走一段弧长，这个新的端点发生的变化怎么用==1;;向量去描述==？
==1;;这个点到圆心的距离不变，但是角度变化了，变化在角度上，也就是说从圆心到端点的向量发生了角度变化，其他不变。旋转这个向量==
<!--SR:!2026-11-18,134,270-->

curve 的extend函数，需要注意传进去的参数或者点坐标，需要按着这个曲线的==1;;走向==去延长(圆，直线，样条)。函数中需要传经的bool代表==1;;起点和端点==，用==1;;true和false==区分。
<!--SR:!2026-12-27,157,270-->


`GetOffsetCurves` 用于==1;;生成曲线的等距偏移结果==。它返回集合而不是单个对象，是因为偏移结果不保证==1;;与原对象保持相同的类型==、==1;;数量==和==1;;拓扑连接关系==。对于多段线，偏移通常需要经历“分段偏移—修剪/补缝—重建边界”的过程，因此结果可能是一个对象，也可能是多个对象。
<!--SR:!2026-09-27,93,250-->

#csharp-flashcards 

C# 统一规范：正则表达式全部写 ==1;;`@"规则"`==，因为==1;;反斜杠是转义符==
<!--SR:!2027-02-01,181,270-->


