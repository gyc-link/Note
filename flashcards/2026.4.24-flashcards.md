
#cad开发-flashcards 

面域的布尔运算，只要调用成功，otherregion会==1;;被delete掉==，函数描述里也说了。并且调用后的结果，比如交、并、差需要我们自己去判断，因此不要==1;;把调用成功当成几何成立==。
<!--SR:!2027-02-04,198,310-->

cad的延长，合并，面域差集命令==extend、join、subtract==
<!--SR:!2026-09-07,84,276-->

AutoCAD .NET API 的bolcktable类，有public的构造函数，是无参的，首先他会通过acHeapAlloc函数申请一块24字节大小的内存，ac是==1;;cad相关的前缀==，heap是==1;;堆==，allocate是==1;;分配==，接着会创建了一个底层的AcDbBlockTableRecord对象。其他的有参构造函数，通过显示调用基类的构造函数一层层 base(...)，把“底层 C++ 对象指针”和“是否自动释放”一路交给基类管理。
<!--SR:!2026-11-01,130,296-->

样条曲线的次数和阶数，控制点的概念，什么是非周期，什么是有理？
?
次数是多项式的最高次数项的次数，阶数是次数+1。
控制点可以从一根直线是两个点控制，圆、椭圆是三个控制点，
周期不是光闭合就行的，首位连接处也需要连续的，光滑的，圆、椭圆这种。
rational，有理，权重。椭圆就是通过不同的控制点间对线的吸引力大小不同实现从抛物线变成椭圆的。有理就是这个吸引力大小，也叫做权重。
<!--SR:!2026-09-14,91,276-->

异常是运行时，因此cad的异常类在==CAD的Runtime==命名空间里
<!--SR:!2026-10-28,127,296-->

eKeyNotFound ,==1;;没有找到键值==，看到异常后尝试自己去理解
eGeneralModelingFailure，==1;;生成模型失败==
eNotApplicable，==1;;对这个对象做了不支持的操作==
<!--SR:!2027-03-20,211,284-->


默认情况下，`DBText` 的 `Position` 不是文字中心，而是文字的==1;;**插入基点**。==通常是左对齐。使用==1;;AligenmentPoint==让字体去瞄准要对准的点。
<!--SR:!2026-11-22,136,276-->

CAD 屏幕上的图形来源可以有两类：
?
1. 数据库对象经过图形管线显示出来
2. 程序临时绘制出来，不保存为独立数据库对象
<!--SR:!2027-02-13,189,277-->

`Curve.Area` 属性的存在是因为什么？
?
它对“曲线”的定义包含了一种**数学上的闭合假设**
为什么要有，为了降低代码的复杂性，没必要为了圆的面积特地访问到curve的下一级去。
<!--SR:!2026-11-07,128,307-->

tolerance是为了解决==1;;浮点误差的==
<!--SR:!2026-10-06,100,285-->

point3d 是==1;;struct，纯数据结构==
pointentity3d是==1;;class，抽象了点的身份==
PointOnCurve3D(子类)额外封装了==1;;“点在曲线上的拓扑关联”==
<!--SR:!2026-10-11,89,264-->

#补充-flashcards 

什么是dry理论？如果变量名已经很清楚了，写注释的时候就==1;;不要重复表达==，应该==1;;表达出变量名和代码本身看不出来的东西==
<!--SR:!2026-11-14,135,290-->

24 字节是一块 24 格的小柜子。一个 int 要占 4 格。所以这块柜子最多能放 ==1;;6== 个 int。底层原因是数据解释：==1;;同一块内存可以被解释成 int、double、指针、结构体==
<!--SR:!2026-10-07,112,296-->

封装的前提是抽象，抽象的本质是==使用目前c#提供的工具，属性，方法等去创造一个东西出来==
<!--SR:!2026-11-11,137,296-->

er加在**动词后** → 表==1;;「人 / 物：执行者、做… 的人 / 工具」==
<!--SR:!2027-04-14,224,276-->

补码，正数是源码，负数是==1;;符号位不变，其他位取反后再加一==
int型整数，-1的补码是：
1、十六进制:==1;;0xFFFFFFFF==
2、二进制:==1;;1111 1111 1111 1111 1111 1111 1111 1111==
<!--SR:!2026-10-05,110,296-->

guid是为了解决什么问题，字节数多大？是因为什么GUID不能代替Git中的哈希计算？
?
- GUID 解决了让海量数据在复杂、分散的网络中拥有唯一的身份证。
- 占用16字节
- GUID没法做到每次对同一个内容生成的ID保持一致。而GIT中每次提交中的哈希值是根据文件内容计算出来的。
<!--SR:!2026-09-13,65,264-->

cad的两套id系统
?
autocad为了让objectid能做到运行时唯一，把这个id跟==内存地址挂钩了==
handle是保证在数据库中唯一，是一个自增的long，8字节
<!--SR:!2026-11-12,118,264-->

#csharp-flashcards 

c#字典的初始化、两种添加元素的方式、访问元素、安全获取值、检查键是否存在、移除元素、遍历
?
```csharp
0、初始化 
var students = new Dictionary<int, string>();
//如果值是list等需要初始化的对象，也需要初始化。
1、添加元素 
students.Add(101, "张三");//这种方式如果键已存在，则会报错
// 思考为什么add方法会报错
// 如果不报错，因为key值相同，那么散列函数计算得出的数组下标是相同的，如果键已存在，插入必然会覆盖旧值，为了区别覆盖（下面的方式），c#选择报错。
students[102] = "李四"; // 这种方式如果键已存在，则会覆盖旧值
2、访问元素
string name = students[101]; 
3、安全获取值
students.TryGetValue(103, out string result)
4、检查键是否存在
bool hasKey = students.ContainsKey(101);
5、移除元素
students.Remove(101);
6、遍历
字典Dictionary的遍历是通过foreach语句，拿到键/值对儿。
```
<!--SR:!2026-12-11,139,267-->


