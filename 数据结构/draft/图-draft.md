---
date: 2026-06-04
tags:
  - 数据结构
status: draft
---
## 图的定义

>[!TIP] 没有重边、自环 的 简单图

>[!NOTE] 图(Graph)
>由顶点集合V和边集合E（顶点之间的连接关系）组成的数据结构，通常记作G = (V,E)

![](../../pic/Pasted%20image%2020260610160521.png)

>顶点 Vertex  ：图的基本单元，表示对象或节点。顶点集合V是有限且非空的，包含n>0个顶点。通常用圆圈表示顶点。
>边  Edge ：连接两个顶点的线段，表示他们之间的关系。边可记作 $e = <u,v>$，表示u到v的一条边，u是起点，v是终点。

>子图(Sub Graph): 如果图 G' = (V',E') 满足 $V' \subset V$且$E' \subset E$，则G'是G的子图。

![](../../pic/Pasted%20image%2020260610161125.png)


>[!TIP] 图的顶点集不能为空，边集可以为空


## 图的分类

### 无向图与有向图

按边是否有方向

- 无向图  边没有方向，常用于表示朋友关系、城市间双向道路等。无向图的边由两个顶点组成的无序对表示，边用圆括号表示
![274](../../pic/Pasted%20image%2020260604154313.png)
- 有向图 边有方向，常用于表示任务流程、依赖关系等。有向图的边（弧）由两个顶点组成的有序对表示，边用尖括号表示
![277](../../pic/Pasted%20image%2020260604154322.png)
![](../../pic/Pasted%20image%2020260610162336.png)

>[!NOTE] 完全图
>如果图有n个顶点，则达到最大边数的图称为完全图
>- 无向图最大边数：完全有向图边数的一半
>- 有向图最大边数：n*（n - 1）每个顶点都有n-1条从自己出发的边

>[!NOTE] 顶点的度
>- 无向图中，顶点的度：与该顶点相连的边的数量，记为T D($v_i$)
>- 有向图中，顶点的度分别为：
>	- 出度 Out Degree：以该顶点为起点的边数，记为O D($v_i$)
>	- 入读 In Degree：以该顶点为终点的边数，记为I D($v_i$)
>	- 顶点总度 T D($v_i$) = O D($v_i$) + I D($v_i$)

### 环形图和无环图

>路径：路径是图论中的核心概念。对于图G = (V,E)，如果存在顶点序列 $v_i0,v_i1,...,v_im$，使得任意相邻顶点对之间都存在边相连，则称该顶点序列为从$v_i0$到$v_im$得一条路径。$v_i0$起点，$v_im$终点

>[!NOTE] 环和简单路径
>环（Cycle）：如果一条路径的起点和终点重合（即$v_i0 = v_im$，则称该路径为 环 或 回路）
>简单路径：路径上所有顶点均不重复出现，除非是环的首尾重合，称为简单路径。

根据图中是否至少存在一个环，将图分为 环形图和无环图

有向无环图 Directed Acyclic Graph 
DAG结构再动态规划、最短路径、数据压缩算法中有着广泛应用。
![](../../pic/Pasted%20image%2020260610173048.png)
### 连通图和非连通图

#### 连通无向图

在无向图中，如果从顶点$v_i$能通过一条路径到达顶点$v_j$，则称$v_i$和$v_j$连通

连通无向图：任意两个顶点之间都有路径相连的无向图

>[!important] 非连通无向图：**存在至少一对顶点之间没有路径相连的无向图**。

![](../../pic/Pasted%20image%2020260610174314.png)

#### 无向图的连通分量

在无向图中，整体可能不是连通的，但其中的某些子图是连通的，这些子图称为连通子图。如果一个连通子图无法再包含于更大的连通子图中，则称其为连通分量，即无向图中的极大连通子图。

- **连通子图**：无向图的一个子图，且该子图是连通的。
- **极大连通子图**：连通子图中，如果不存在包含它的更大的连通子图，则称为极大连通子图。
- **连通分量**：无向图中的极大连通子图，即为该图的连通分量。

上图中的非连通无向图中，顶点$v_1$，$v_2$，$v_3$，$v_4$及其相连的边构成了一个连通子图，且无法再扩展为更大的连通子图，因此它是原图的一个连通分量。同理，右边的子图是原图的另一个连通分量。

#### 强连通有向图

在有向图中，如果顶点$v_i$能到达 $v_j$，且$v_j$也能到达$v_i$，则称 $v_i$ 和$v_j$是「强连通」的。

- **强连通有向图**：任意两个顶点都能互相到达的有向图。
- **非强连通有向图**：存在至少一对顶点<span style="color:red;">不能互相到达</span>的有向图。
![](../../pic/Pasted%20image%2020260610175155.png)

#### 有向图的强连通分量

在有向图中，「强连通分量」指的是：图中某个极大子图，子图内任意两个顶点都可以互相到达（即强连通），并且无法再加入其他顶点使其仍然强连通。

简要定义如下：

- **强连通子图**：有向图的一个子图，子图内任意两点互相可达。
- **极大强连通子图**：不能再加入其他顶点的强连通子图。
- **强连通分量**：有向图中的极大强连通子图。

类似于无向图的连通分量概念，只是加了强用于区分。
上图中的非强连通有向图中，除了顶点$v_7$，其他顶点及其边构成了一个强连通分量。顶点$v_7$自身也单独构成一个强连通分量。

### 带权图

有些图不仅表示顶点之间是否有关系，还需要描述这种关系的「强度」或「代价」，这就是「权」。权值可以表示距离、时间、费用等。

- **带权图**：每条边都带有权值的图。权值一般为非负数，有时也可以为负数。
- **网络**：带权且连通的无向图称为网络。

![](../../pic/Pasted%20image%2020260610181458.png)
### 稀疏/稠密图

图按边的多少可分为「稠密图」和「稀疏图」，这一划分没有严格的界限，仅为便于理解。

- **稠密图（Dense Graph）**：边数接近完全图的图，即大多数顶点之间都有边相连。
- **稀疏图（Sparse Graph）**：边数远少于完全图的图（常见如 e<n×log⁡2ne<n×log2​n），大部分顶点之间没有直接连接。


## 图的存储结构

在实现图的存储时，关键在于如何高效地表示顶点与边之间的关系。

常见的图存储方式主要分为「顺序存储结构」和「链式存储结构」两大类。

顺序存储结构包括邻接矩阵和边集数组；

链式存储结构则有邻接表、链式前向星、十字链表、邻接多重表等。

### 邻接矩阵

#### 邻接矩阵的原理描述

>[!NOTE] 邻接矩阵（Adjacency Matrix）
>通过一个二维数组A来表示顶点之间的连接关系。
>- 对于无权图，如果`A[i-1][j-1] == 1`，表示顶点$v_i$与$v_j$之间有边；如果为0，表示两者之间无边。
>- 对于带权图，如果 $A[i-1][j-1] == w \quad \text{且} \quad w \neq \infty$,则表示顶点$v_i$与$v_j$之间有一条权值为w的边；如果为无穷，则表示两者之间无边。

![](../../pic/Pasted%20image%2020260610184859.png)

>[!NOTE] 可以发现无向图的邻接矩阵是对称的

邻接矩阵的初始化和遍历效率低，因此不能很快的得出边的数量
空间占用大，空间复杂度是n方
但是实现容易，能快速判断两个顶点间是否存在边，能快速得到某个顶点的度、出度、入度。（看第i行和第i列）

### 邻接表

#### 邻接表的原理描述

**邻接表（Adjacency List）**：一种结合顺序存储与链式存储的图结构。
主要由两部分组成：
- 用于存放所有顶点信息的<span style="color:red;">数组</span>
- 用于存放每个顶点所有邻接边的<span style="color:red;">链表</span>。


为了便于随机访问任意顶点的邻接链表，通常将所有顶点节点以数组形式顺序存储，数组下标即为顶点在图中的编号。

每个顶点在邻接表中都有一个表头节点（即「顶点节点」），该节点包含顶点本身的信息和指向其第一条邻接边的指针。这样可以高效地访问和管理每个顶点的所有邻接边。

以有向带权图为示例，每个链表节点存储边的终点节点、边的权值和指向下一个边节点的指针（除了头节点）

![](../../pic/Pasted%20image%2020260610191806.png)


#### 邻接表的代码实现

```csharp
class GraphAdjacencyList<T>
    {
        class EdgeNode 
        {
            public  double val;
            public T endVertex;
            public EdgeNode next;
            public EdgeNode(T vj, double val)
            {
                this.endVertex = vj;
                this.val = val;
                next = null;
            }
        }
        class VertexNode 
        {
            private T vi;

            /// <summary>
            /// 指向该顶点的第一条邻接边
            /// </summary>
            public EdgeNode first;
            public VertexNode(T vi)
            {
                this.vi = vi;
                first = null;                     
            }
        }

        private int vertexCount;
        private bool isDirected;
        private T[] vertexs;
        private VertexNode[] graph;

        public GraphAdjacencyList(T[] vertexs)
        {
            if (vertexs == null)
            {
                vertexCount = 0;
                graph = new VertexNode[0];
                return;
            }

            vertexCount = vertexs.Length;
            this.vertexs = vertexs;
            graph = new VertexNode[vertexCount];
            for (int i = 0; i < vertexCount; i++)
            {
                graph[i] = new VertexNode(vertexs[i]);
            }         
        }


        /// <returns>返回-1说明该顶点不在图的顶点中</returns>
        private int GetVertexIndex(T v)
        {
            if (v == null || graph == null) return -1;

            for(int i = 0;i< graph.Length; i++)
            {
                if (vertexs[i].Equals(v))
                    return i;
            }
            return -1;
        }

        private bool isValid(T v)
        {
            return !(GetVertexIndex(v) == -1);
        }

        public void AddEdge(T vi, T vj, double val = 1)
        {
            // 抛出具体的参数异常，并写明原因
            if (!isValid(vi))
                throw new ArgumentException($"起点顶点 '{vi}' 在图中不存在。");

            if (!isValid(vj))
                throw new ArgumentException($"终点顶点 '{vj}' 在图中不存在。");

            // 头插法
            var edge = new EdgeNode(vj, val);
            var head = graph[GetVertexIndex(vi)];

            edge.next = head.first;
            head.first = edge;
        }


        /// <returns>返回0时代表没有边</returns>
        public double GetEdgeVal(T vi, T vj)
        {
            // 同样验证结点有效性，限于篇幅省略

            var cur = graph[GetVertexIndex(vi)].first;
            while (cur != null)
            {
                if (cur.endVertex.Equals(vj))
                {
                    return cur.val;
                }
                cur = cur.next;
            }
            return 0;
        }

        /// <summary>
        /// 遍历顶点 vi 的所有邻接边 (vj, val)
        /// </summary>
        public IEnumerable<(T vj, double val)> GetNeighbors(T vi)
        {
            var cur = graph[GetVertexIndex(vi)].first;
            while (cur != null)
            {
                yield return (cur.endVertex, cur.val);
                cur = cur.next;
            }
        }

        /// <summary>
        /// 打印所有边，按顶点顺序
        /// </summary>
        public void PrintGraph()
        {
            for(int i = 0; i < vertexCount; i++)
            {
                var cur = graph[i].first;
                while (cur !=null)
                {
                    Console.WriteLine(cur.val);               
                    cur = cur.next;
                }                     
            }
        }
    }
```


>[!NOTE] 代码分析
>见[图-邻接表补充](图-邻接表补充.md)

#### 邻接表的算法分析

- **时间复杂度分析**：
    - **图的初始化与创建**：O(n+m)，其中 n 表示顶点数，m 表示边数。因为需要为每个顶点分配空间，并依次插入每条边。
    - **查询是否存在 vi​ 到 vj​ 的边**：O(TD(vi))，其中 TD(vi​) 表示顶点 vi​ 的出度。由于邻接表只存储与 vi​ 直接相连的边，因此需要遍历 vi​ 的所有邻接点，最坏情况下需遍历 vi 的全部出边。
    - **遍历某个顶点的所有边**：O(TD(vi​))，即与该顶点相连的所有边都需访问一遍，效率较高。
    - **遍历整张图的所有边**：O(n+m)。遍历所有顶点，每个顶点的邻接表总共包含 m 条边，因此整体遍历代价为 O(n+m)。
- **空间复杂度分析**：O(n+m)，邻接表需要为每个顶点分配一个链表头结点O(n)），并为每条边分配一个边节点 O(m)），因此总空间复杂度为 O(n+m)。相比邻接矩阵，邻接表在稀疏图中能显著节省空间。


### 链式前向星

**链式前向星（Linked Forward Star）**，又称静态邻接表，是一种以静态链表实现邻接表的高效图存储结构。它将**边集数组与邻接表**结合，能够高效地访问某个节点的所有邻接点，并且空间开销极小。

>[!note] 静态链表
>所谓静态链表就是用数组的下标（Index）隐式充当内存地址，用数组内部存储的整数（Integer）来模拟指针（Pointer）的跳转。

其核心由两类数据结构组成：

- **边集数组** edges：$edges[i]$表示第 i 条边，包含以下信息：$edges[i].vj$为该边的终点，$edges[i].val$ 为该边的权值，$edges[i].next$指向与该边起点相同的下一条边在 edges 数组中的下标。
- **头节点数组** head：$head[i]$ 存储以顶点 i 为起点的第一条边在 $edges$ 数组中的下标。

示例：

![](../../pic/Pasted%20image%2020260613184118.png)
1、头节点数组的存储
- 首先，没有顶点$v_0$，因此数组第一个格子里存-1
- 数组第二个格子下标是1，因此存以顶点$v_1$为起点的第一条边在 $edges$ 数组中的下标，存1
- 数组第三个格子下标是2，存以顶点$v_2$为起点的第一条边在 $edges$ 数组中的下标，存2
- 数组第四个格子下标是3，存以顶点$v_3$为起点的第一条边在 $edges$ 数组中的下标，存4
- 以此类推

2、以遍历顶点 v1​ 的所有出边为例，步骤如下：

- 首先，通过 `index = head[1] = 1`，找到以 v1​ 为起点的第一条边在 `edges` 数组中的下标，即 `edges[1]`，对应边 ⟨v1​,v5​⟩，权值为 6。
- 然后，根据 `index = edges[1].next = 0`，找到第二条边 `edges[0]`，即 ⟨v1​,v2​⟩，权值为 5。
- 最后，`index = edges[0].next = -1`，表示没有更多的出边，遍历结束。

### 哈希表实现邻接表

利用哈希表（即字典）高效地实现邻接表结构。

具体做法是：使用一个字典存储所有顶点信息，字典的键为顶点编号，值为该顶点的邻接边集合（同样用一个字典表示）。每个顶点对应的邻接边字典，其键为相邻顶点（有向图则是边的终点）的编号，值为对应边的权重。

这样既方便查询某一顶点的所有出边，也便于获取任意一条边的权重

## 深度优先搜索

### 简介

**深度优先搜索算法（Depth First Search，简称 DFS）**：是一种用于遍历或搜索树、图等结构的经典算法。其核心思想是「沿一条路径尽可能深入」，遇到无法继续的节点时再回溯到上一个分叉点，继续探索其他路径，直到遍历完整个结构或找到目标为止。

>一条路走到黑
>可以从任何一个点开始遍历

>[!tip] 遍历序列结果不唯一

在 DFS 的遍历过程中，通常需要**暂存当前节点 u 的相邻节点**，以便回溯时继续访问未遍历的节点。由于遍历顺序具有「后进先出」的特性，DFS 可以通过递归（系统栈）或显式使用栈结构来实现

### 算法步骤

以无向图为例
1. 选定起始节点 u，将其标记为已访问。
2. 判断当前节点 u 是否为目标节点（具体依据题目要求）。
3. 如果 u 为目标节点，直接返回结果。
4. 如果 u 不是目标节点，则遍历 u 的所有未被访问的邻接节点。
5. 对每一个未访问的邻接节点 v，递归地从 v 继续进行深度优先搜索。
6. 如果 u 没有未访问的邻接节点，则回溯到上一个节点，继续探索其他分支。
7. 重复步骤 2∼6，直到遍历完整个图或找到目标节点为止。

示例：

![](../../pic/Pasted%20image%2020260611182834.png)

>如果从v2出发，只能遍历到v5，因此需要再从其他结点出发，再进行深度优先遍历。

>[!NOTE] 如果是无向图，则执行几次深度优先搜索，图中就有几个连通分量

### 基于递归的深度优先搜索

深度优先搜索（DFS）可以通过递归方式实现。其基本递归流程如下：

1. 设 grap 为无向图的邻接表，visited 为已访问节点的集合，u 为当前节点。定义递归函数 `def dfs_recursive(graph, u, visited):`。
2. 将起始节点 u 标记为已访问（`visited.add(u)`）。
3. 判断当前节点 u 是否为目标节点（具体依据题目要求）。
4. 如果 u 为目标节点，直接返回结果。
5. 如果 u 不是目标节点，则遍历 u 的所有未访问的邻接节点 v。
6. 对每个未访问的邻接节点 v，递归调用 `dfs_recursive(graph, v, visited)` 继续搜索。
7. 如果 u 没有未访问的邻接节点，则自动回溯到上一个节点，继续探索其他分支。
8. 重复步骤 3∼7，直到遍历完整个图或找到目标节点为止

## 广度优先搜索

**广度优先搜索算法（Breadth First Search，简称 BFS）**：是一种用于遍历或搜索树、图等结构的经典算法。BFS 从起始节点出发，按照层级逐步向外扩展，优先访问距离起点较近的节点，再访问距离较远的节点，直到遍历完整个结构或找到目标节点。



![](../../pic/Pasted%20image%2020260617092846.png)

```
     0
   /    \
   1     2
   /       \
   3        4
   /5

```
如上，按层扩展


Breadth First Search

搜索是算法，用不同搜索的算法去遍历

层层扩散

## 最小生成树

生成树 Spanning Tree
对于一个无向连通图G，如果它的一个子图既包含G的所有顶点，又是一棵树（连通且无环），那么这个子图就叫做G的生成树。生成树不是唯一的，可能得到不同的生成树

1. **包含所有顶点**：生成树必须覆盖原图的所有顶点。
2. **连通性**：生成树是连通的，任意两个顶点之间都能互相到达。
3. **无环性**：生成树中没有环。
4. **边数最少**：生成树的边数总是等于顶点数减 11，即 n−1n−1 条边。

prim算法

随便取一个顶点，找离它最近的另一个顶点，把这个顶点加入进来，接着再找下一个离他们最近的顶点，直到把所有的顶点都加入进来。


适合稠密图
为什么，因为是找点为核心

kruskal算法

在所有的边中，找最小权值的边，加入到边集合中，每次加入一条边都要看这条边的两端的顶点是否已经连通，如果已经连通则不需要加入这条边。直到所有的顶点连通

以找边为核心

最小生成树不一定唯一，但是边权和是最小的，唯一的。

## 单源最短路径

在一个带权图G = 中，给定一个起点，找从这个源点出发，到图中其他所有顶点的最短路径长度。

### Dijkstra 算法

每次选出距离起点最近，最短路尚未确定的结点，用它去尝试更新其他结点的最短距离，逐步扩展，直到所有节点的最短路径都确定。

>[!note] 贪心策略
>贪心策略（Greedy Strategy）是一种在每一步选择中都采取当前状态下最好或最优（最有利）的选择，从而希望导致结果是全局最好或最优的算法设计策略。




## 找环 Cycle Detection

### 无向图

DFS + parent node

union find

把edge的两端端点放入同一个集合中，后面的edge如果两端端点在这个集合中，说明找到了环


## 寻找最小环


问题：网格状，有顶点，有边的平面图，寻找该平面图的全部最小环

思路一
第一步：从一个顶点出发，搜索它的顶点，直到出现重复的顶点，说明找到一个环
第二部：将图视为两部分，第一步拿到的环和其余部分，按分治算法思想递归调用，直到没有更小的环，过程中记录所有的最小环

思路二
从一个顶点出发，每次都走最左的边，最后形成的闭合回路一定是最小的环。


## 闪卡

#DataStructure-flashcards

图的两种常用的存储结构：
?
- 邻接矩阵：通过一个二维数组来表示顶点之间的连接关系
- 邻接表：一种结合顺序存储与链式存储的图结构，主要由两部分组成
	- 用于存放所有顶点信息的数组
	- 用于存放每个顶点所有邻接边的链表
<!--SR:!2026-09-28,58,250-->